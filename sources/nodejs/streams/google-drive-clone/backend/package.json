{
  "name": "@google-drive-clone/backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "mkdir -p downloads && npx nodemon src/index.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules npx jest --runInBand",
    "test:watch": "npm test -- --watch",
    "test:cov": "npm test -- --no-cache --coverage"
  },
  "dependencies": {
    "pino": "6.8",
    "pino-pretty": "5.1",
    "pretty-bytes": "5.6",
    "socket.io": "4.1"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "nodemon": "2.0"
  }
}
