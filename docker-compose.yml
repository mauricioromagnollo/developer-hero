version: '3'

services:
  # db:
  #   image: postgres
  #   container_name: doe_backend_postgres
  #   restart: always
  #   tty: true
  #   ports:
  #     - '${POSTGRES_PORT}:5432'
  #   environment:
  #     POSTGRES_HOST: ${POSTGRES_HOST}
  #     POSTGRES_PORT: ${POSTGRES_PORT}
  #     POSTGRES_USERNAME: ${POSTGRES_USERNAME}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #     POSTGRES_DATABASE: ${POSTGRES_DATABASE}

  app:
    image: doe_backend
    build: .
    tty: true
    # links:
    #   - db
    volumes:
      - .:/doe-backend
      - ./node_modules:/doe-backend/node_modules
    ports:
      - ${PORT}:3000
    # depends_on:
    #   - db
    restart: always
    command: bash -c "npm run dev"

  # pgadmin:
  #   image: dpage/pgadmin4
  #   tty: true
  #   environment:
  #     - PGADMIN_DEFAULT_EMAIL=${PGADMIN_EMAIL}
  #     - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_PASSWORD}
  #   ports:
  #     - "${PGADMIN_PORT}:80"
  #   depends_on:
  #     - db
